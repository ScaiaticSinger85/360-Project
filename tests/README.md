# Tests

## How to Run

### Server API Tests (requires server running on localhost:4000)

```bash
# From the project root:
node tests/server.test.js
```

### Unit Tests (no server required)

```bash
node tests/unit.test.js
```
